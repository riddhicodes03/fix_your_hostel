import 'package:flutter/material.dart'; // Use this ONLY to define your seed colors and any truly static colors.

// Never reference this in widgets.
class AppSeeds {
  static const Color darkSeed = Color(0xFF9CA9F5); // your primary
  static const Color lightSeed = Color(
    0xFF3D4A9E,
  );
  static const Color bgLight =Color(0xFF1C2333); // darker variant for light mode
}
